# Secrets — every .env variant, but keep the template
.env*
!.env.example

# Local benchmark resources — real machine-specific paths, never exposed
benchmarks/test_set.py
benchmarks/.quality-cache/

# Environments
.venv/
venv/

# Caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

node_modules/
.next/
out/

# Build output: `npm run build` in ui/ stages the GUI into the package it ships from
src/standup/web/

# Local project index, artifacts and decks
.standup/

# The deck README's build step writes here. Not *.pptx — the layout template is source.
/deck.pptx

# Editors
.vscode/
.idea/
.claude/settings.local.json

docs/
CLAUDE.md
AGENTS.md
.codex

.DS_Store
Thumbs.db
